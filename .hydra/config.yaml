root_dir: ${env:CLIPORT_ROOT}
tag: default
debug: false
mode: test
agent: mae_sep_base
n_demos: 100
train_demos: 1000
n_repeats: 1
gpu:
- 0
save_results: true
update_results: true
checkpoint_type: last
val_on_heldout: true
disp: false
shared_memory: false
eval_task: packing-omni-objects
model_task: packing-omni-objects
type: single
model_dir: ${root_dir}
exp_folder: exps_cliport/0419_voltron
data_dir: /home/robot/Repositories_chaoran/CLIPort_new_loss/data_debug
assets_root: ${root_dir}/cliport/environments/assets/
model_path: ${model_dir}/${exp_folder}/${model_task}-${agent}-n${train_demos}-train/checkpoints/
train_config: ${model_dir}/${exp_folder}/${model_task}-${agent}-n${train_demos}-train/.hydra/config.yaml
save_path: ${model_dir}/${exp_folder}/${eval_task}-${agent}-n${train_demos}-train/checkpoints/
results_path: ${model_dir}/${exp_folder}/${eval_task}-${agent}-n${train_demos}-train/checkpoints/
sep: false
model_path_1: ${model_dir}/${exp_folder}/${model_task}-${agent}-pick-n${train_demos}-train/checkpoints/
model_path_2: ${model_dir}/${exp_folder}/${model_task}-${agent}-place-n${train_demos}-train/checkpoints/
record:
  save_video: false
  save_video_path: ${model_dir}/${exp_folder}/${eval_task}-${agent}-n${train_demos}-train/videos/
  add_text: true
  fps: 20
  video_height: 640
  video_width: 720
